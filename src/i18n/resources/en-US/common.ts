const common = {
  window: {
    newNote: "New note",
    openSettings: "Settings",
    minimize: "Minimize",
    maximize: "Maximize",
    restore: "Restore",
    close: "Close",
  },
} as const;

export default common;
